name: Android Release

on:
  workflow_dispatch:
  push:
    tags: ['v*.*.*']

permissions:
  contents: write

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi
      - uses: android-actions/setup-android@v3
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0" "ndk;28.2.13676358"
      - run: npm ci
      - run: npm run check
      - name: Prepare signing key
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          test -n "$KEYSTORE_BASE64" || { echo "ANDROID_KEYSTORE_BASE64 is required"; exit 1; }
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/savewave-release.jks"
      - name: Build signed ARM64 APK
        env:
          SAVEWAVE_ANDROID_KEYSTORE: ${{ runner.temp }}/savewave-release.jks
          SAVEWAVE_ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          SAVEWAVE_ANDROID_STORE_PASSWORD: ${{ secrets.ANDROID_STORE_PASSWORD }}
          SAVEWAVE_ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: npm run tauri -- android build --target aarch64 --apk
      - name: Preserve ARM64 APK
        shell: bash
        run: |
          APK="$(find src-tauri/gen/android/app/build/outputs/apk -type f -path '*/release/*.apk' -printf '%T@ %p\n' | sort -nr | head -n 1 | cut -d' ' -f2-)"
          test -n "$APK" || { echo "Signed ARM64 APK was not produced"; exit 1; }
          cp "$APK" Savewave-android-arm64.apk
      - name: Build signed ARMv7 APK for older phones
        env:
          SAVEWAVE_ANDROID_KEYSTORE: ${{ runner.temp }}/savewave-release.jks
          SAVEWAVE_ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          SAVEWAVE_ANDROID_STORE_PASSWORD: ${{ secrets.ANDROID_STORE_PASSWORD }}
          SAVEWAVE_ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: npm run tauri -- android build --target armv7 --apk
      - name: Preserve ARMv7 APK
        shell: bash
        run: |
          APK="$(find src-tauri/gen/android/app/build/outputs/apk -type f -path '*/release/*.apk' -printf '%T@ %p\n' | sort -nr | head -n 1 | cut -d' ' -f2-)"
          test -n "$APK" || { echo "Signed ARMv7 APK was not produced"; exit 1; }
          cp "$APK" Savewave-android-armv7.apk
      - name: Verify and checksum
        shell: bash
        run: |
          APKSIGNER="$ANDROID_HOME/build-tools/$(ls "$ANDROID_HOME/build-tools" | sort -V | tail -n 1)/apksigner"
          "$APKSIGNER" verify --verbose Savewave-android-arm64.apk
          "$APKSIGNER" verify --verbose Savewave-android-armv7.apk
          sha256sum Savewave-android-arm64.apk Savewave-android-armv7.apk > Savewave-android.apk.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: savewave-android
          path: |
            Savewave-android-arm64.apk
            Savewave-android-armv7.apk
            Savewave-android.apk.sha256
      - name: Publish tagged release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            Savewave-android-arm64.apk
            Savewave-android-armv7.apk
            Savewave-android.apk.sha256
