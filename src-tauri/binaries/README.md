# Managed desktop sidecars

Release automation must place architecture-suffixed `yt-dlp` and FFmpeg executables here before a desktop bundle is built. Tauri requires names such as `yt-dlp-x86_64-pc-windows-msvc.exe` and `ffmpeg-x86_64-pc-windows-msvc.exe`. Binaries are intentionally not committed.
